#include "main.h"
/**
 * swap_int - Entry point
 * swap_int:  swaps the values of two integers
 * @a: first integer
 * @b: second integer
 * Return: Nothing
 */
void swap_int(int *a, int *b)
{
	int first, second;

	first = *a;
	second = *b;

	printf("a = %d", second);
	printf("b = %d", first);
}
