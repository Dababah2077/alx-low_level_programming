#include "main.h"
/**
 * _strlen - Entry point
 * _strlen: returns the length of a string
 * @s: checks input of function
 * Return: Nothing
 */
int _strlen(char *s)
{
	int str;

	str = *s;
	strlen(str);
}
