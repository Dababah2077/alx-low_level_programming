#include "main.h"
/**
 * _abs - Entry point
 * _abs: A program computes the absolute value of an integer
 * @n: input
 * Return: the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
