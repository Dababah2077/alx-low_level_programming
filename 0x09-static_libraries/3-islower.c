#include "main.h"
/**
 * _islower - Entry point
 * _islower: A program checks for lowercase character
 * @c: input
 * Return: 1 if c is lowercase, otherwise 0
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
